#!/bin/bash
# Example script for server.py to test.

# If there is no output, check the args passed to server.py
# and the "date" command. on the two machines; they must
# have their time sync'd.

echo "Hello world."
echo "First argument: $1"
